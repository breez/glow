import 'package:flutter/material.dart';

class BreezColors {
  // Core brand colors - using exact hex values
  static const primary = Color(0xFF055deb);
  static const primaryDark = Color(0xFF00091c);
  static const primaryLight = Color(0xFF0085fb);

  // Light theme colors
  static const lightBackground = Color(0xFF055deb);
  static const lightSurface = Color(0xFFf9f9f9);
  static const lightCard = Colors.white;

  // Dark theme colors
  static const darkBackground = Color(0xFF00091c);
  static const darkSurface = Color(0xFF0A1428);
  static const darkCard = Color(0x520A1428);

  // Accent colors
  static const warning = Color(0xffffe685);
  static const warningDark = Color(0xFFeddc97);

  // Greys
  static const grey500 = Color(0xFF9E9E9E);
  static const grey600 = Color(0xFF757575);
  static const grey900 = Color(0xFF212121);
}
