import 'package:flutter/material.dart';

class BreezColors {
  // Core brand colors - using exact hex values
  static const Color primary = Color(0xFF055deb);
  static const Color primaryDark = Color(0xFF00091c);
  static const Color primaryLight = Color(0xFF0085fb);

  // Light theme colors
  static const Color lightBackground = Color(0xFF055deb);
  static const Color lightSurface = Color(0xFFf9f9f9);
  static const Color lightCard = Colors.white;

  // Dark theme colors
  static const Color darkBackground = Color(0xFF00091c);
  static const Color darkSurface = Color(0xFF0A1428);
  static const Color darkCard = Color(0x520A1428);

  // Accent colors
  static const Color warning = Color(0xffffe685);
  static const Color warningDark = Color(0xFFeddc97);

  // Greys
  static const Color grey500 = Color(0xFF9E9E9E);
  static const Color grey600 = Color(0xFF757575);
  static const Color grey900 = Color(0xFF212121);
}
